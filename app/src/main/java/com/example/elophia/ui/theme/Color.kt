package com.example.elophia.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════
// ELOPHIA BRAND — NAVY + GOLD
// ═══════════════════════════════════════════════

// ─── Navy family ───────────────────────────────
val Navy = Color(0xFF0D1B2A)          // Deep navy — dark bg, text
val NavyCard = Color(0xFF1B3A6B)      // Card / surface navy
val NavyLight = Color(0xFF2A4A7B)     // Lighter navy (hover, variant)
val NavyDark = Color(0xFF08121C)      // Deepest navy

// ─── Gold family ───────────────────────────────
val Gold = Color(0xFFE5B95C)          // Primary gold — buttons, accents
val GoldDark = Color(0xFFC99A3D)      // Darker gold (pressed state)
val GoldLight = Color(0xFFF0D08A)     // Lighter gold (hover)

// ─── Light mode ────────────────────────────────
val CreamBackground = Color(0xFFF5EFE0)   // App background in light mode
val CreamSurface = Color(0xFFFDFBF5)      // Slightly lighter cream

// ─── Dark mode ─────────────────────────────────
val DarkBackground = Color(0xFF0D1B2A)    // App background in dark mode
val DarkSurface = Color(0xFF1A2A44)       // Card surface in dark mode

// ─── Text ──────────────────────────────────────
val TextPrimaryLight = Color(0xFF0D1B2A)  // On cream
val TextSecondaryLight = Color(0xFF6B7280)
val TextPrimaryDark = Color(0xFFF5EFE0)   // On dark navy
val TextSecondaryDark = Color(0xFFA0A8B8)
val TextOnNavy = Color(0xFFF5EFE0)        // On navy cards
val TextOnGold = Color(0xFF0D1B2A)        // On gold buttons

// ─── Status ────────────────────────────────────
val ErrorRed = Color(0xFFD32F2F)
val ErrorRedDark = Color(0xFFEF5350)
val SuccessGreen = Color(0xFF2E7D32)
val SuccessGreenDark = Color(0xFF66BB6A)

// ─── Borders / dividers ────────────────────────
val GoldBorder = Color(0xFFE5B95C)
val DividerLight = Color(0xFFE0D8C4)
val DividerDark = Color(0xFF2A3A5A)